'use client';

import NotFoundHero from '@/components/landing/not-found-hero';

export default function NotFound() {
    return (
        <main className="min-h-screen w-full bg-background">
            <NotFoundHero />
        </main>
    );
}
